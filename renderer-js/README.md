# andrep — JavaScript/TypeScript Renderer

Renders AndRep JSON templates to HTML (browser and Node.js).

## Install

```bash
npm install github:USER/andrep#path=renderer-js
```

## Status

Work in progress — not yet usable.

## Architecture

See `../_docs/ARCHITECTURE_ANDREP.md` for the full template format and renderer contract.
