# andrep — Python Renderer

Renders AndRep JSON templates to HTML and PDF (via WeasyPrint).

## Install

```bash
pip install git+https://github.com/USER/andrep.git#subdirectory=renderer
```

## Status

Work in progress — not yet usable.
